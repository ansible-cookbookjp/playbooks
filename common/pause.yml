---
- hosts: localhost
  gather_facts: false
  connection: ansible.builtin.local

  tasks:
    - name: pause 10 seconds
      ansible.builtin.pause:
        seconds: 10

    - name: pause 1 minutes
      ansible.builtin.pause:
        minutes: 1
